{
  "name": "whatsdrink-miniprogram",
  "version": "1.0.0",
  "private": true,
  "description": "WhatsDrink 原生微信小程序",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "validate:project": "node scripts/validate-project.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run validate:project && npm run typecheck && npm test"
  },
  "dependencies": {
    "tdesign-miniprogram": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "miniprogram-api-typings": "^4.1.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
