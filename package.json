{
  "name": "shiguangji-miniprogram",
  "version": "1.0.0",
  "private": true,
  "description": "拾光迹 - 个人足迹地图微信小程序",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "validate:project": "node scripts/validate-project.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run validate:project && npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "miniprogram-api-typings": "^4.1.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
